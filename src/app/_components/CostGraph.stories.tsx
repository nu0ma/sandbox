import { StoryObj } from "@storybook/react";
import { CostGraph } from "./CostGraph";

export default {
  component: CostGraph,
  title: "CostGraph",
};

type Story = StoryObj<typeof CostGraph>;

export const Default: Story = {};
